import Foundation
import SoraFoundation

struct QRScannerViewDisplayParams {
    let topTitle: LocalizableResource<String>?
    let details: LocalizableResource<TitleIconViewModel>?
    let scanTitle: LocalizableResource<String>
}
